import { HighlightIPipe } from './highlight-i.pipe';

describe('HighlightIPipe', () => {
  it('create an instance', () => {
    const pipe = new HighlightIPipe();
    expect(pipe).toBeTruthy();
  });
});
